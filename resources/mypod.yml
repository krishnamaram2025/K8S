apiVersion: v1
kind: Pod
metadata:
    name: my-pod
    labels: 
      env: Test
      server: web
spec:
     containers:
     - name: http 
       image: http
       ports:
       - containerPort: 80
